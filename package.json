{
  "name": "stellarbridge-fusion-plus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "contracts:compile": "pnpm --filter @stellarbridge/ethereum-contracts compile",
    "contracts:test": "pnpm --filter @stellarbridge/ethereum-contracts test",
    "relayer:dev": "pnpm --filter @stellarbridge/relayer dev",
    "frontend:dev": "pnpm --filter frontend dev",
    "setup": "pnpm install && pnpm build",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "devDependencies": {
    "@jest/globals": "^30.0.5",
    "@playwright/test": "^1.54.1",
    "@types/jest": "^30.0.0",
    "jest": "^30.0.5",
    "prisma": "^6.12.0",
    "ts-jest": "^29.4.0",
    "turbo": "^2.5.5"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "ioredis": "^5.6.1"
  }
}
